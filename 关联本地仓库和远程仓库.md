关联远程和本地
```
git remote add origin git@github.com:trailblazer887/pylearing-project.git(参考地址，不要复制)
git branch -M main
git push -u origin main
```
推送和拉取
```
git push [远程仓库名] [本地分支名]:[远程分支名]
git push -u origin branch-name # 首次推送新分支，并建立追踪关系
git pull [远程仓库名] [远程分支名]
```